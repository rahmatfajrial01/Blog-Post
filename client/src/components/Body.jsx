import React from 'react'

const Body = ({ children }) => {
    return (
        <section className='container mx-auto min-h-screen'>
            {children}
        </section>
    )
}

export default Body